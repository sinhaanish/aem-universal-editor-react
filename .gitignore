# dependencies
node_modules/

# build output
build/

# local env files with real credentials — never commit these
.env.development.local
.env.local
.env.production.local
.env.test.local

# App Builder — contains runtime credentials and private keys
.env
.aws.tmp.creds.json

# Build artifacts
.parcel-cache/
dist/

# misc
.DS_Store
